// Standalone layout — bypasses the admin shell so the invite page
// renders without sidebar, MFA gate, or role requirements.
export default function AcceptInviteLayout({ children }) {
  return children
}
